---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const tecnologiaEntries = await getCollection('tecnologia');
---

<BaseLayout title="Tecnología" description="Software y herramientas desarrolladas en el Laboratorio de Dinámica No Lineal">
  <main class="min-h-screen bg-white dark:bg-gray-900">
    <!-- Hero Section -->
    <section class="bg-gradient-to-r from-unam-blue-600 to-accent-purple-600 text-white py-20">
      <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto text-center">
          <h1 class="text-4xl md:text-5xl font-display font-bold mb-6 animate-fade-in">
            Tecnología
          </h1>
          <p class="text-xl text-white/90 animate-fade-in" style="animation-delay: 0.2s">
            En todas nuestras investigaciones las herramientas de software son fundamentales. 
            Hemos desarrollado diversas tecnologías de software libre para la docencia y la investigación.
          </p>
        </div>
      </div>
    </section>

    <!-- Software Grid -->
    <section class="py-16">
      <div class="container mx-auto px-4">
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-7xl mx-auto">
          {tecnologiaEntries.map((entry, index) => (
            <a 
              href={`/tecnologia/${entry.slug}`}
              class="card p-6 group animate-fade-in"
              style={`animation-delay: ${0.1 * index}s`}
            >
              {entry.data.screenshots && entry.data.screenshots[0] && (
                <div class="mb-4 rounded-lg overflow-hidden">
                  <img 
                    src={entry.data.screenshots[0]} 
                    alt={entry.data.title}
                    class="w-full h-48 object-cover transform group-hover:scale-105 transition-transform duration-300"
                  />
                </div>
              )}
              
              <div class="flex items-start justify-between mb-2">
                <h2 class="text-2xl font-display font-bold text-gray-900 dark:text-white group-hover:text-unam-blue-500 dark:group-hover:text-accent-cyan-500 transition-colors">
                  {entry.data.title}
                </h2>
                {entry.data.version && (
                  <span class="px-2 py-1 bg-unam-blue-100 dark:bg-unam-blue-900 text-unam-blue-700 dark:text-unam-blue-300 text-xs font-semibold rounded">
                    v{entry.data.version}
                  </span>
                )}
              </div>
              
              <p class="text-gray-600 dark:text-gray-400 mb-4">
                {entry.data.description}
              </p>
              
              {entry.data.features && (
                <div class="space-y-1 mb-4">
                  {entry.data.features.slice(0, 3).map(feature => (
                    <div class="flex items-start gap-2 text-sm text-gray-600 dark:text-gray-400">
                      <svg class="w-4 h-4 text-green-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                      </svg>
                      <span>{feature}</span>
                    </div>
                  ))}
                </div>
              )}
              
              <div class="flex items-center text-unam-blue-500 dark:text-accent-cyan-500 font-medium group-hover:underline">
                Ver más
                <svg class="w-5 h-5 ml-1 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                </svg>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- Call to Action -->
    <section class="py-16 bg-gray-50 dark:bg-gray-800">
      <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto text-center">
          <h2 class="text-3xl font-display font-bold text-gray-900 dark:text-white mb-4">
            Software Libre
          </h2>
          <p class="text-lg text-gray-600 dark:text-gray-400 mb-8">
            Todas nuestras herramientas son de código abierto y están disponibles para uso académico y de investigación.
          </p>
          <p class="text-gray-600 dark:text-gray-400">
            Para soporte técnico o reportar problemas, contacta al laboratorio.
          </p>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>
